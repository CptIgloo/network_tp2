#include "replication_manager.hpp"

void ReplicationManager::Replicate(MemoryStream stream,std::vector<GameObject*> objects){
    for(GameObject* g: objects){
        
        //g->Write(stream);

    }

}